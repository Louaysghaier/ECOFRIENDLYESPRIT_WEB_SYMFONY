{% extends 'base.html.twig' %}

{% block title %}Add Post!{% endblock %}

{% block body %}
<section class="contact-us" id="contact">
    <div class="form-container">
    <h2> Add Your Comment!</h2>
    <table>

        {# Ajoutez la classe "form-container" à l'élément du formulaire #}
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        <div class="align">
        <tr>
            <div class="form-group">
                <div class="formu-Postlabel">
                    {{ form_label(form.descriptionComment) }}
                </div>
                {{ form_widget(form.descriptionComment) }}
            </div>
            {{ form_errors(form.descriptionComment, { attr: { class: 'text-danger' }, only: true }) }}
        </tr>
        <tr> 
            <div class="form-group">
            {#<li data-filter="*"><a href="{{ path('Readcommentfront', {'postId': c.getIdPost()}) }}">{{ form_row(form.Share) }}</a></li>#}
            <li data-filter="*"><a href="{{ path('Readcommentfront', {'postId': postId }) }}">{{ form_row(form.Share) }}</a></li>


                {#{{ form_row(form.Published) }}#}
            </div>
        </tr>
    </table>
        {{ form_end(form) }}
    </div>
    </div>
</section>
{% endblock %}