{% extends 'base.html.twig' %}
{% block title %} List Comments {% endblock %}
{% block body %}
<section class="contact-us" id="contact">
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <div class="row">
             <div class="col-lg-12">
                <div class="filters">
                  <div style="text-align: right;  padding:20px 20px 50px 20px; display: flex; ">
                    <div class="list-container" style="text-align: center;">
                      {% for comment in c %}
                      <div class="card" style="margin-bottom: 20px; width: 1100px; height: auto;">
                        <div class="down-content">
                           <h6 style=" text-align: right; padding :15px; color: #B22222">{{ comment.dateCreationComment|date('Y-m-d')}}</h6>
                            <p style=" padding-left  :25px; text-align: left;">{{ comment.descriptionComment }}</p>
                        {#{% if post.imagePost is not null %}
                          <div style="display: flex; justify-content: center; align-items: center;">
                            <img src="{{ asset(post.getImagePost()) }}" alt="" style="max-width: 300px; max-height: 300px ;">
                          </div> 
                        {% endif %}
                        <div style="display: flex; align-items: center;">
                          <ul style="display: flex; gap: 10px; margin-right: 10px;">
                              <li data-filter="*" style="padding-right: 20px;"><a href="{{ path('addComment', {'postId': post.getId()}) }}">Add Comment</a></li>
                              <li data-filter="*"><a href="{{ path('Readcommentfront', {'postId': post.getId()}) }}">Read Comment</a></li>
                          </ul>
                          <p style="padding-left: 600px; margin-right: 10px;">Number of Comments: {{ post.getNbresComments() }}</p>
                        </div>#}
                      </div> 
                    </div>  
                 {% endfor %}
                 </div>
               </div>
             </div>
           </div>
         </div>
       </div>
      </div>
    </div>
  </div>
</section>
{% endblock %}