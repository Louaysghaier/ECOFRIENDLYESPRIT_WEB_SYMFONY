{% extends 'base.html.twig' %}

{% block title %}Hello CodePromoController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
  <table border="1">
                    <thead>
                    <tr>     
                        <th>pourcentage</th>
                        <th>idevent</th>
                         <th>idtransport</th>
                          <th>idservice</th>
                          <th>Suppression</th>
                       
                       
                    </tr>
                    </thead>
                    <tbody>
                    {% for codepromo in b %}
                        <tr>
                            <td>{{ codepromo.pourcentage}}</td>
                            <td>{{ codepromo.idevent }}</td>
                            <td>{{ codepromo.idtransport}}</td>
                            <td>{{ codepromo.idservice }}</td>
                            
                            <td>
                                <a class="btn btn-danger" href="{{ path('sup_promo',{'idcodepromo':codepromo.idcodepromo}) }}">Supprimer</a>
                            </td>
                            
                        </tr>

                    {% endfor %}

                    </tbody>
                </table>
</div>
{% endblock %}
