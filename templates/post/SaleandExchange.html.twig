{#{% extends 'base.html.twig' %}

{% block title %}Welcome to Forum!{% endblock %}
{% block body %}
<section class="heading-page header-text" id="top">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h6>Welcome to Forum</h6>
                <h2>Sale & Exchange</h2>
            </div>
        </div>
    </div>
</section>

 

<section class="meetings-page" id="meetings">
  {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
  {% endfor %}
   {% for message in app.flashes('danger') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
  {% endfor %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="filters">
                            <div style="text-align: right;  padding:20px 20px 50px 20px; display: flex; ">
                              <div class="search-form">
                                <form action="{{ path('search') }}" method="get">
                                  <div class="form-group">
                                      <input type="text" name="query" placeholder="Search" style="padding: 10px; font-size: 16px;
                                      border: 1px solid #ccc; border-radius: 5px; width: 250px; transition: border-color 0.3s ease;">
                                      <button type="submit" class="red-button"style="background-color: #ff0000; color: #fff; padding: 10px 20px;
                                      font-size: 16px;border: none; border-radius: 5px; cursor: pointer; transition: background-color 0.3s ease;">Search</button>
                                  </div>
                              </form>
                              </div>
                            </div>
                            <ul>
                                <li data-filter="*"><a href="{{ path('addPost') }}">Add Post</a></li>
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="list-container" style="box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);">
                  {% for post in p %}
                    <div class="card" style="margin-bottom: 20px; width: 1100px; height: auto; box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);">
                      <div class="down-content">
                        <h6 style=" text-align: right; padding :15px; color: #B22222">{{ post.dateCreationPost|date('Y-m-d') }}</h6>
                        <h4 style="color: black; padding-left :60px; text-align: left;">{{ post.title }}</h4>
                        <p style=" padding-left  :25px; text-align: left;">{{ post.descriptionPost }}</p>
                        {% if post.imagePost is not null %}
                          <div style="display: flex; justify-content: center; align-items: center;">
                            <img src="{{ asset(post.getImagePost()) }}" alt="" style="max-width: 300px; max-height: 300px ;">
                          </div> 
                        {% endif %}
                        <div style="display: flex; align-items: center;">
                          <ul style="display: flex; gap: 10px; margin-right: 10px;">
                              <li data-filter="*" style="padding-right: 20px;"><a href="{{ path('addComment', {'postId': post.getId()}) }}">Add Comment</a></li>
                              <li data-filter="*"><a href="{{ path('Readcommentfront', {'postId': post.getId()}) }}">Read Comment</a></li>
                          </ul>
                          <p style="padding-left: 500px; margin-right: 10px;">Number of Comments: {{ post.getNbresComments() }}</p>
                        </div>
                      </div> 
                    </div>
                    
                 {% endfor %}
              </div>
              {{ knp_pagination_render(p) }}
            </div>
        </div>
    </div>
</section>
{% endblock %}#}

{% extends 'base.html.twig' %}
{# {% block stylesheet %}
<style>
</style>
{% endblock %} #}
{% block title %}Welcome to Forum!{% endblock %}
{% block body %}
<section class="heading-page header-text" id="top">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h6>Welcome to Forum</h6>
                <h2>Sale & Exchange</h2>
            </div>
        </div>
    </div>
</section>

<section class="meetings-page" id="meetings">
    {% for message in app.flashes('success') %}
    <div class="alert alert-success">
        {{ message }}
    </div>
    {% endfor %}
    {% for message in app.flashes('danger') %}
    <div class="alert alert-danger">
        {{ message }}
    </div>
    {% endfor %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="filters">
                            <div style="text-align: right;  padding:20px 20px 50px 20px; display: flex; ">
                                <div class="search-form">
                                    <form action="{{ path('search') }}" method="get">
                                        <div class="form-group">
                                            <input type="text" name="query" placeholder="Search"
                                                style="padding: 10px; font-size: 16px;
                                      border: 1px solid #ccc; border-radius: 5px; width: 250px; transition: border-color 0.3s ease;">
                                            <button type="submit" class="red-button"
                                                style="background-color: #ff0000; color: #fff; padding: 10px 20px;
                                      font-size: 16px;border: none; border-radius: 5px; cursor: pointer; transition: background-color 0.3s ease;">Search</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                            <ul>
                                <li data-filter="*"><a href="{{ path('addPost') }}">Add Post</a></li>
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="list-container" style="box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);">
                    {# {% set sortedPosts = p|sort((a, b) => b.dateCreationPost.timestamp - a.dateCreationPost.timestamp) %} #}
                    {% for post in p %}
                    <div class="card" style="margin-bottom: 20px; width: 1100px; height: auto; box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);">
                        <div class="down-content">
                            <h6 style=" text-align: right; padding :15px; color: #B22222">{{ post.dateCreationPost|date('Y-m-d') }}</h6>
                            <h4 style="color: black; padding-left :60px; text-align: left;">{{ post.title }}</h4>
                            <p style=" padding-left  :25px; text-align: left;">{{ post.descriptionPost }}</p>
                            {% if post.imagePost is not null %}
                            <div style="display: flex; justify-content: center; align-items: center;">
                                <img src="{{ asset(post.getImagePost()) }}" alt=""
                                    style="max-width: 300px; max-height: 300px ;">
                            </div>
                            {% endif %}
                            <div style="display: flex; align-items: center;">
                                <ul style="display: flex; gap: 10px; margin-right: 10px;">
                                    <li data-filter="*" style="padding-right: 20px;"><a
                                            href="{{ path('addComment', {'postId': post.getId()}) }}">Add
                                            Comment</a></li>
                                    <li data-filter="*"><a
                                            href="{{ path('Readcommentfront', {'postId': post.getId()}) }}">Read
                                            Comment</a></li>
                                </ul>
                                <p style="padding-left: 500px; margin-right: 10px;">Number of Comments:
                                    {{ post.getNbresComments() }}</p>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                


